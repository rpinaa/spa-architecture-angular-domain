import { NormalImagePipe } from './normal-image.pipe';

describe('NormalImagePipe', () => {
  it('create an instance', () => {
    const pipe = new NormalImagePipe();
    expect(pipe).toBeTruthy();
  });
});
