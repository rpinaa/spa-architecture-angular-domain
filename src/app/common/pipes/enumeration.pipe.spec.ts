import { EnumerationPipe } from './enumeration.pipe';

describe('EnumerationPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumerationPipe();
    expect(pipe).toBeTruthy();
  });
});
